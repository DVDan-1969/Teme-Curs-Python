"""5. Doi vectori a și b:
a = np.array([1, 2, 3])
 = np.array([4, 5, 6])b
Calculează: a + b, a * b, a.dot(b) și a ** 2."""

import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
print(a + b)
print(a*b)
print(a.dot(b))
print(a**2)

